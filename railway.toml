# ─────────────────────────────────────────────────────────────
#  railway.toml — Dark Lavalink V4
#  Railway.app deployment configuration
#  © 2025 Dark (SynthX Development)
# ─────────────────────────────────────────────────────────────

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Restart automatically if Lavalink crashes
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Health check — Railway pings this to confirm the service is alive
healthcheckPath = "/version"
healthcheckTimeout = 300

# Railway will send SIGTERM before killing — gives Lavalink time to shut down cleanly
numReplicas = 1
